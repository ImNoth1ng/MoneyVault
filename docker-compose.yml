version: '3.8'

services:
  # ==========================================
  # Service 1: MySQL Database (Aislado)
  # ==========================================
  finanzas-db:
    image: mysql:8.0
    container_name: moneyvault-db
    restart: always
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-finanzas_db}
      MYSQL_USER: ${MYSQL_USER:-finanzas_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-finanzas_pass_2026}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root_pass_2026}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - moneyvault-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-root_pass_2026}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ==========================================
  # Service 2: Spring Boot Backend API
  # ==========================================
  finanzas-api:
    build:
      context: ./finanzas
      dockerfile: Dockerfile
    container_name: moneyvault-api
    restart: always
    depends_on:
      finanzas-db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://finanzas-db:3306/${MYSQL_DATABASE:-finanzas_db}?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-finanzas_user}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-finanzas_pass_2026}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      JWT_SECRET: ${JWT_SECRET:-PPp27xSTfBwOpRn4/AV6gPzQSnQg+Oi80KdWfCcuAHs=}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}
    ports:
      - "${BACKEND_PORT:-8085}:8085"
    networks:
      - moneyvault-net

  # ==========================================
  # Service 3: React Frontend + Nginx Proxy
  # ==========================================
  finanzas-ui:
    build:
      context: ./finanzas-ui
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api/v1
    container_name: moneyvault-ui
    restart: always
    depends_on:
      - finanzas-api
    ports:
      - "${APP_PORT:-8888}:80"
    networks:
      - moneyvault-net

networks:
  moneyvault-net:
    driver: bridge

volumes:
  mysql_data:
    driver: local
